# Development targets. Defaults are LOCAL — see ../../dev-env.sh
# Copy to .env.local (git-ignored) only if you need to change them.

# Origin the dev /api proxy forwards to.
# Set to https://cloudyadminapi.azaken.com to work against PRODUCTION DATA.
VITE_API_TARGET=http://localhost:5055

# Site loaded into the live-preview iframe.
VITE_PORTFOLIO_ORIGIN=http://localhost:5202
